{
  "name": "agencia-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate && npx prisma db push --accept-data-loss",
    "start": "cd backend && node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
